import { ZalgoDraggableDirective } from './zalgo-draggable.directive';

describe('ZalgoDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new ZalgoDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
